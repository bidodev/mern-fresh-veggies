// ===== INDEX for REDUCER ===

import { combineReducers } from 'redux';

const allReducers = combineReducers({
  //  <name>: <reducer name>,
  // <name>: <reducer name>,
});

export default allReducers;
